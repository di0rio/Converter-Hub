# Image fixtures

Synthetic files for the Image tool (`/image`).

- `sample.svg` — a 120×80 drawing that also carries a `<script>` and an external
  `<image>`. Converted through `<img>`, neither runs nor loads.
