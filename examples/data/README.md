# Synthetic data fixtures

Invented records for the data tool (`/data`). The `people.*` files hold the
same two rows in every input format, so a test can read each one and expect the
same table. `nested.json` is deliberately not a table.
